# Scope work units for /discover
- **Scout sweep**: Codebase + external source scan for existing patterns. Disjoint per-domain. Run in parallel sub-agents.
- **Describe pass**: Interactive problem exploration with user (PPT, visualization, validation). Sequential. Requires user session.
- **Specify pass**: AC derivation from problem statement. Interactive with user. Sequential per group.
